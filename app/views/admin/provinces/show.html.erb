<h1>Province: <%= @province.name %></h1>

<div>
  <label>Name</label>
  <input type="text" value="<%= @province.name %>" disabled>
</div>

<div>
  <label>Description</label>
  <textarea><%= @province.description %></textarea>
</div>
