<h1>Province: <%= @province.name %></h1>
<div class="row">
<%= button_to "Edit", edit_admin_province_path, method: :get, class: "btn btn-primary" %>
<%= button_to "Delete", admin_province_path, method: :delete,
    data: {confirm: "Are you sure?"}, class: "btn btn-danger" %>
</div>
<div>
  <%= @province.description %>
</div>
